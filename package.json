{
  "name": "metaverse-adventure-mobile",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --open /dev.html",
    "build": "tsc && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "export:html": "npm run build && node scripts/make-standalone.mjs"
  },
  "dependencies": {
    "phaser": "^3.90.0",
    "vite": "^6.0.0"
  },
  "devDependencies": {
    "playwright-core": "^1.60.0",
    "typescript": "^5.7.2"
  }
}
